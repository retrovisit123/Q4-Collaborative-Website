>git add . 
- Adds all changes to staging area

>git commit -m "commit"
- Saves all changes in staging area to the document

>git push origin
- Pushes saved changes to the main repository

>git pull
- Loads changes pushed by other collaborators documents